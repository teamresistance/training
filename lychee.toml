# Exclude localhost URLs and Hugo template syntax
exclude = [
    '^http://localhost',
    '{{.*}}',  # Exclude Hugo template variables
]

# Only check external absolute URLs
include = ['^https?://']

# Exclude checking files in certain paths
exclude_path = ['layouts/']
