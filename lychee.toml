# Exclude localhost URLs
exclude = [
    '^http://localhost',
    # Exclude all root-relative paths (like /motors/, /abstract-io/, etc.)
    '^\/'
]

# Include only absolute URLs (http/https) for checking
include = ['^https?://']
